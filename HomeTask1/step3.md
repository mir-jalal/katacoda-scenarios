You can get status information by:
`sudo systemctl status devops_service`{{execute}}

You can enable it by:
`sudo systemctl enable devops_service`{{execute}}

You can stop it by:
`sudo systemctl stop devops_service`{{execute}}
